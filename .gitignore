# Dependencies
node_modules/

# Build output (rebuild with `npm run build` after clone)
dist/

# Local env (secrets — never commit)
.env
.env.local
.env.*.local

# Logs and caches
*.log
npm-debug.log*
.vite/
.cache/

# Editor / OS
.DS_Store
Thumbs.db
